<%= form_with(model: term, local: true) do |form| %>
  <% if term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(term.errors.count, "error") %> prohibited this term from being saved:</h2>
      <ul>
        <% term.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :schema_name, "Şema" %>
    <%= form.text_field :schema_name %>
  </div>

  <div class="field">
    <%= form.label :table_name, "Tablo" %>
    <%= form.text_field :table_name %>
  </div>

  <div class="field">
    <%= form.label :column_name, "Kolon" %>
    <%= form.text_field :column_name %>
  </div>

  <div class="field">
    <%= form.label :description, "Açıklama" %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
